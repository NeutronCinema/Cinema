[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Cinema-docs"
authors = [{name = "cinema-developers", email = "cinema-users@outlook.com"}]
dynamic = ["version", "description"]
